1.随机访问元素：vector deque;
2.容器中间插入或删除元素 list 
3.在容器首部或是尾部插入或是删除元素 deque
4.读取输入时在容器的中间插入元素，之后随机访问元素，考虑在输入时将元素读入到list，之后重排序，使其适合顺序访问，让后将将list复制到vector 
5.list无法用下标访问元素，所以避免在其他两种容器中用下标和随机访问
6.pop_front不能用于 vector
7.list迭代器不支持运算

